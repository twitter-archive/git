#!/bin/sh

name=$1; shift

c_quote () {
	sed 's/\\/\\\\/g; s/"/\\"/g'
}

cat <<-EOF
#ifndef MAKE_${name}_H
#define MAKE_${name}_H

/* Auto-generated by mkcstring */

#define MAKE_${name} "$(c_quote)"

#endif /* MAKE_${name}_H */
EOF
